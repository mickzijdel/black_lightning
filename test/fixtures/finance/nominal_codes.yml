# == Schema Information
#
# Table name: finance_nominal_codes
#
# *id*::          <tt>bigint, not null, primary key</tt>
# *code*::        <tt>string(255), not null</tt>
# *name*::        <tt>string(255), not null</tt>
# *description*:: <tt>text(65535), not null</tt>
# *active*::      <tt>boolean, not null</tt>
# *created_at*::  <tt>datetime, not null</tt>
# *updated_at*::  <tt>datetime, not null</tt>
#--
# == Schema Information End
#++
sundry:
  code: 439999
  name: "Sundry Expenditure"
  description: "The catch-all for Bedlam expenses. Fiona Winters wants to move away from it."
  active: true

set_and_tech:
  code: 3331111
  name: "Set and Tech Expenditure"
  description: "Expenses for set and tech purposes."
  active: true

inactive:
  code: 900800
  name: "Inactive Code"
  description: "This is just for testing. Make sure the default excludes it."
  active: false

income:
  code: 359000
  name: "Other Income"
  description: "Generic Income Nominal Code"
  active: true
